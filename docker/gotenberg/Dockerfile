FROM gotenberg/gotenberg:8

# Copy optional custom fonts into the image to improve document fidelity.
# Add .ttf/.otf files under docker/gotenberg/fonts and rebuild the image.
COPY docker/gotenberg/fonts/ /usr/local/share/fonts/flexiconvert/
