services:
  gotenberg:
    build:
      context: .
      dockerfile: docker/gotenberg/Dockerfile
    image: flexiconvert-gotenberg:local
    container_name: flexiconvert-gotenberg
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
